{
  "name": "finally-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "FinAlly end-to-end tests. Playwright on the host against http://localhost:8000.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "typecheck": "tsc --noEmit",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3"
  }
}
